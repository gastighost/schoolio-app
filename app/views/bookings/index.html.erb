
// my dashboard

<div class="container">
<% @bookings.each do |booking| %>
<p><%= booking.id %></p>
<%= link_to "Cancel Booking",
                  booking_path(booking),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %>
<% end %>
</div>

