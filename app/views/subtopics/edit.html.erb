<div class="container mt-4">
<%= simple_form_for [@course, @subtopic] do |f| %>
  <strong><%= f.input :description %></strong>
  <%= f.submit "Save", class: "button_green" %>
<% end %>
<%= link_to "Back to course", course_path(@course), class: "button_yellow" %>
</div>
